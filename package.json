{
  "name": "my-vitepress-project",
  "version": "1.0.0",
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vitepress": "^1.2.3"
  },
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "preview": "vitepress preview docs",
    "deploy": "npm run build && gh-pages -d ../../docs-dist/dist"
  }
}